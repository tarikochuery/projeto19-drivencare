import { Router } from "express";

const doctorRoutes = Router();

// doctorRoutes.post('/signup', schemaValidation(doctorSchema.signup), doctorController.create);
// doctorRoutes.post('/signin', schemaValidation(doctorSchema.signin), doctorController.login);
// doctorRoutes.get('/appointment', appointmentController.getDoctor);
// doctorRoutes.post('/appointment', schemaValidation(doctorSchema.appointment), appointmentController.confirm);
// doctorRoutes.get('/appointment/history', appointmentController.getHistory);

export default doctorRoutes;